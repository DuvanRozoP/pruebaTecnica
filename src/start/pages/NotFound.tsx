import MainNavbar from '@components/layout/MainNavbar';
import { FC } from 'react';

const NotFound: FC = () => {
  return (
    <MainNavbar>
      <h1>No Found</h1>
    </MainNavbar>
  );
};

export default NotFound;
