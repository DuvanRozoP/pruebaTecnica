Within this folder, you will create the following files:

all the services that make an API call, creating them as reused functions:

Ej:
GetAllUsers

// import axios from 'axios';

async function GetAllUsers(url) {
  try {
    const response = await axios.get(url);
    return response.data;
  } catch (error) {
    console.error('Error en la solicitud GET:', error);
    throw error;
  }
}

export default get;
